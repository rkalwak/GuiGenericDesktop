<Window x:Class="GuiGenericBuilderDesktop.Z2SVersionPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wybierz wersję firmware Z2S Gateway"
        Height="570" Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock FontSize="22" FontWeight="Bold" Text="🔧" VerticalAlignment="Center"/>
            <TextBlock FontSize="18" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"
                       Text="Wybierz wersję do wgrania"/>
            <Button x:Name="LoadReleasesButton" Content="Pobierz listę"
                    Width="110" Height="28" Margin="20,0,6,0"
                    VerticalAlignment="Center"
                    Background="#4CAF50" Foreground="White" FontWeight="SemiBold"
                    Click="LoadReleasesButton_Click" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#388E3C"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button x:Name="RefreshReleasesButton" Content="Odśwież (GitHub)"
                    Width="130" Height="28" Margin="0,0,0,0"
                    VerticalAlignment="Center"
                    Background="#FF9800" Foreground="White" FontWeight="SemiBold"
                    Click="RefreshReleasesButton_Click" Cursor="Hand"
                    ToolTip="Wyczyść pamięć podręczną i pobierz świeżą listę z GitHub">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E65100"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Current device version info -->
        <TextBlock Grid.Row="1" x:Name="CurrentVersionText"
                   FontSize="13" Foreground="#555" Margin="0,0,0,10"
                   Text="Aktualna wersja na urządzeniu: —"/>

        <!-- Release list -->
        <ListBox Grid.Row="2" x:Name="ReleaseListBox"
                 Margin="0,0,0,10"
                 SelectionChanged="ReleaseListBox_SelectionChanged"
                 BorderBrush="#CCCCCC" BorderThickness="1"
                 Background="White">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding TagName}" FontWeight="SemiBold" FontSize="14"/>
                            <TextBlock Text="{Binding Name}" FontSize="11" Foreground="#666"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding PublishedAt, StringFormat='{}{0:yyyy-MM-dd}'}"
                                   FontSize="11" Foreground="#888" VerticalAlignment="Center" Margin="12,0"/>
                        <Border Grid.Column="2" x:Name="LatestBadge" Background="#4CAF50"
                                CornerRadius="4" Padding="6,2" Visibility="Collapsed">
                            <TextBlock Text="NAJNOWSZA" FontSize="10" Foreground="White" FontWeight="Bold"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Downgrade warning -->
        <Border Grid.Row="3" x:Name="DowngradeWarningBorder"
                Background="#FFF3CD" BorderBrush="#FFCA2C" BorderThickness="1"
                CornerRadius="4" Padding="10,8" Margin="0,0,0,10"
                Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="16" Text="⚠️" VerticalAlignment="Center"/>
                <TextBlock Margin="8,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center"
                           Text="Wybrana wersja jest starsza niż najnowsza dostępna. Przejście na starszą wersję może powodować problemy z działaniem urządzenia."/>
            </StackPanel>
        </Border>

        <!-- Loading indicator -->
        <TextBlock Grid.Row="3" x:Name="LoadingText" Text="Pobieranie listy wersji z GitHub…"
                   FontStyle="Italic" Foreground="#888" Margin="0,0,0,10"
                   Visibility="Visible"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="FlashButton" Content="Wgraj wybraną wersję"
                    Width="170" Height="35" Margin="0,0,10,0"
                    IsEnabled="False"
                    Background="#2196F3" Foreground="White" FontWeight="SemiBold"
                    Click="FlashButton_Click" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4" BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Anuluj" Width="100" Height="35"
                    Click="CancelButton_Click" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="BorderBrush" Value="#CCCCCC"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
