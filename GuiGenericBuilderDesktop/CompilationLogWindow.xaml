<Window x:Class="GuiGenericBuilderDesktop.CompilationLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Compilation Logs" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Compilation Failed - Error Logs" 
                   FontSize="16" 
                   FontWeight="Bold" 
                   Margin="0,0,0,12"/>

        <!-- Log Display -->
        <Border Grid.Row="1" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1" 
                CornerRadius="4"
                Background="White">
            <TextBox x:Name="LogTextBox"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="11"
                     Padding="8"
                     Background="White"
                     AcceptsReturn="True"/>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,12,0,0">
            <Button x:Name="CopyButton"
                    Content="Copy to Clipboard"
                    Width="140"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="CopyButton_Click"/>
            <Button x:Name="SaveButton"
                    Content="Save to File..."
                    Width="120"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="SaveButton_Click"/>
            <Button x:Name="CloseButton"
                    Content="Close"
                    Width="80"
                    Height="32"
                    IsDefault="True"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
